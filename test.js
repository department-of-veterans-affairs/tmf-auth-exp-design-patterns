// CodeQL wont pass if it doesn't find any code in the repo that it can scan
// this file is just to see if this will trigger a scan to successfully run

const foo = bar;
console.log('test.js loaded', foo);

